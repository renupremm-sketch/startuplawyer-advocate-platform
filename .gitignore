# Dependencies
node_modules
.pnp
.pnp.js

# Testing
coverage

# Production
dist
build

# Misc
.DS_Store
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Database (Optional: Keep this in repo if it's just a seed/demo db and you want it on the server initially, but usually ignored)
# advocate.db 
# For this demo, let's keep advocate.db tracked so the deployed server has data.
# If it grows large, we should ignore it.

# Editor directories and files
.idea
.vscode
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
